export const getSupportedMimeType = () => ['audio/webm;codecs=opus', 'audio/webm', 'audio/mp4', 'audio/ogg'].find(type => MediaRecorder.isTypeSupported(type)) || '';
export const recordingService = { async requestStream(options: MediaTrackConstraints = {}) { if (!navigator.mediaDevices?.getUserMedia) throw new Error('MIC_UNAVAILABLE'); return navigator.mediaDevices.getUserMedia({ audio: { echoCancellation: true, noiseSuppression: true, autoGainControl: true, ...options } }); }, createRecorder(stream: MediaStream) { const mimeType = getSupportedMimeType(); return new MediaRecorder(stream, mimeType ? { mimeType } : undefined); } };