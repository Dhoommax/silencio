import type { Recording, Transcript, UserSettings } from '../types';
const DB_NAME = 'silencio-db';
const openDb = (): Promise<IDBDatabase> => new Promise((resolve, reject) => { const request = indexedDB.open(DB_NAME, 1); request.onupgradeneeded = () => { const db = request.result; if (!db.objectStoreNames.contains('recordings')) db.createObjectStore('recordings', { keyPath: 'id' }); if (!db.objectStoreNames.contains('transcripts')) db.createObjectStore('transcripts', { keyPath: 'id' }); if (!db.objectStoreNames.contains('settings')) db.createObjectStore('settings', { keyPath: 'id' }); }; request.onsuccess = () => resolve(request.result); request.onerror = () => reject(request.error); });
const put = async <T extends { id: string }>(store: string, value: T) => { const db = await openDb(); return new Promise<void>((resolve, reject) => { const tx = db.transaction(store, 'readwrite'); tx.objectStore(store).put(value); tx.oncomplete = () => resolve(); tx.onerror = () => reject(tx.error); }); };
const all = async <T>(store: string): Promise<T[]> => { const db = await openDb(); return new Promise((resolve, reject) => { const request = db.transaction(store).objectStore(store).getAll(); request.onsuccess = () => resolve(request.result as T[]); request.onerror = () => reject(request.error); }); };
export const storageService = { saveRecording: (r: Recording) => put('recordings', r), getRecordings: () => all<Recording>('recordings'), saveTranscript: (t: Transcript) => put('transcripts', t), getTranscripts: () => all<Transcript>('transcripts'), saveSettings: (s: UserSettings) => put('settings', { ...s, id: 'current' }), async getSettings() { const values = await all<UserSettings & { id: string }>('settings'); return values[0]; }, async deleteRecording(id: string) { const db = await openDb(); db.transaction('recordings', 'readwrite').objectStore('recordings').delete(id); }, async deleteTranscript(id: string) { const db = await openDb(); db.transaction('transcripts', 'readwrite').objectStore('transcripts').delete(id); } }; 
